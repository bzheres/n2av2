[build]
  base = "frontend"
  publish = "dist"
  command = "npm run build"

# 1) Admin bypass (ONLY you know this path)
[[redirects]]
  from = "/n2a-admin-2d4f9a7c/*"
  to = "/index.html"
  status = 200

# 2) Everyone else sees maintenance
[[redirects]]
  from = "/*"
  to = "/maintenance.html"
  status = 200
